<form method="post" action="{{ path('app_profil_company_delete', {'slug': company.slug}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce compte');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.slug) }}">
    <button class="btn">Supprimer</button>
</form>
